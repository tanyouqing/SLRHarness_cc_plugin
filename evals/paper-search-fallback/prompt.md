---
max_turns: 40
allowed_tools: [Skill, Agent, Read, Glob, Grep, Write, Edit, Bash, PowerShell, WebSearch, WebFetch]
---

继续 `eval-one-round`。我明确批准当前 scope，请开始正式调研。当前环境没有安装
`paper-search` CLI；不要安装它，使用已有的学术 MCP 或 WebSearch/WebFetch 回退。
为了这个评测，将 `maxRounds` 设为 1，并完成最终交付。
