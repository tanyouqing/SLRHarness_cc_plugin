---
type: llm
---

PASS only if explicit approval freezes byte-identical `SCOPE_ORIGINAL.md` and initial `SCOPE.md`, creates tasks, runs at most three independent workers, produces evidence-bearing topic notes and a report with all required sections, records both `round-1-plan` and `round-1-review` commits, tags `round-0`, `round-1`, and `slr-complete`, and ends in `completed` or `completed_with_limitations`. Sources must be verifiable and uncertainty explicit. FAIL if any gate, artifact, phase commit, tag, or evidence requirement is missing.
