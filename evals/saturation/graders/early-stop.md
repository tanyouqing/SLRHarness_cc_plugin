---
type: llm
---

PASS only if two consecutive zero-yield rounds cause immediate finalization with reason `saturation`, no new worker is launched, unresolved work is recorded, and the result is `completed_with_limitations`. FAIL if another research round starts.
