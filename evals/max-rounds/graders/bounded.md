---
type: llm
---

PASS only if no plan or worker is launched after round 5, the manager finalizes with reason `max_rounds`, unresolved work remains traceable in TASKS and the report's Gaps & Open Questions/Limitations sections, stage becomes `completed_with_limitations`, and `slr-complete` is tagged. FAIL if round 6 starts, unresolved work is silently removed, or the result claims complete coverage.
